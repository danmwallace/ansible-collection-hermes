---
- name: Destroy molecule instance
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Stop and remove hermes-gateway-instance container
      containers.podman.podman_container:
        name: hermes-gateway-instance
        state: absent
